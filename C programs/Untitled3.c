#include <stdio.h>
int main(void)
{
    int num1,num2,sum;
    printf("Take any value:");
    scanf("%f",&num1);
    printf("Take any value:");
    scanf("%f",&num2);
    sum=num1+num2;
    printf("The sum is =%f",sum);
    return 0;
}
